/**
 * @file Enums - GreaseNotesType
 * @module grease/enums/GreaseNotesType
 */

/**
 * Types of release notes generated by the `grease-notes` lifecycle.
 */
export enum GreaseNotesType {
  /**
   * Generate notes in birthday (first release) format.
   */
  BIRTHDAY = 'birthday',

  /**
   * Generate empty release notes.
   */
  BLANK = 'blank',

  /**
   * Generate release notes from `CHANGELOG` excerpt.
   */
  CHANGELOG = 'changelog',

  /**
   * Skip release note generation.
   */
  NULL = 'null'
}
