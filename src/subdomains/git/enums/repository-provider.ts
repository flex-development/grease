/**
 * @file Enums - RepositoryProvider
 * @module grease/git/enums/RepositoryProvider
 */

/**
 * Repository provider names.
 *
 * @enum {Lowercase<string>}
 */
enum RepositoryProvider {
  BITBUCKET = 'bitbucket',
  GITHUB = 'github',
  GITLAB = 'gitlab'
}

export default RepositoryProvider
